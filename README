lexとyaccプログラムでC言語の解析

ソース
フォルダー「c_if」
C言語のif文の解析

フォルダー「c_while_if」
C言語のifとwhilen文の解析

フォルダー「c_function_if」
C言語の関数とif文の解析

コンパイル方法
1. flex -l def.l
2. bison -dv -y def.y
3. gcc  y.tab.c lex.yy.c

テスト方法
4. ./a.out < hello.c
5. ./a.out < accept.c
6. ./a.out < deny.c
